{
    "name": "mcp-cli-metadata",
    "version": "1.0.0",
    "description": "Versioned Azure MCP CLI metadata snapshots",
    "private": true,
    "scripts": {
        "snapshot": "node create-version-snapshot.js",
        "test:snapshot": "node --test test/create-version-snapshot.test.js",
        "test": "npm run test:snapshot"
    },
    "engines": {
        "node": ">=22.0.0"
    },
    "license": "ISC",
    "dependencies": {
        "@azure/mcp": "3.0.0-beta.47"
    }
}
